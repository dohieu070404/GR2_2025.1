# Copy to .env and adjust for your environment.
#
# IMPORTANT:
# - Backend default port is 3000 (see backend/.env.example)
# - When testing on a real phone, do NOT use localhost.
#   Use your PC/LAN IP (same network as your phone), e.g.:
#     http://192.168.1.10:3000
# - Android emulator: you can use http://10.0.2.2:3000

# dev | staging | prod
EXPO_PUBLIC_APP_ENV=dev

# Backend base URL
# If you omit this in dev, the app will try to auto-detect the Metro host and use http://<metro-host>:3000
EXPO_PUBLIC_API_URL=http://192.168.1.10:3000
